import random

def strategy(opponent_history):
   
    
    if len(opponent_history) >2 and opponent_history[0] == 'split' and opponent_history[-2] == 'split':
        return random.choice(['split', 'steal'])
    
    return 'steal'
